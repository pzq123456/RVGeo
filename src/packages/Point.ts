/**
 * @description
 * - A point is an ordered pair of numbers written as (x, y) or (x, y, z) depending on the dimension of the space. (L, B, H) in geodesy.
 * - 点是一对有序的数字，写成(x, y)或(x, y, z)，具体取决于空间的维度。 (L, B, H) 或者 (经度, 纬度, 高程) 在大地测量学中。
 */